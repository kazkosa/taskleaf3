<section id="container">
  <%= render 'layouts/admin_sidebar' %>
  <!-- **********************************************************************************************************************************************************
      MAIN CONTENT
      *********************************************************************************************************************************************************** -->
  <!--main content start-->
  <section id="main-content">
    <section class="wrapper">
      <h3><i class="fa fa-angle-right"></i><%= link_to "States", admin_states_path%></h3>
      <div class="row mt">
        <div class="col-lg-12">
          <div class="content-panel">
            <h4><i class="fa fa-angle-right"></i>Edit State</h4>
            <div style="margin: 0 10px 10px 0;text-align: right;">
              <%= link_to "State Infomation", admin_state_path(@state), class:"btn btn-outline-secondary btn-sm"%>
            </div>
            <section id="unseen">
              <%= form_with model: @state, local: true, url: admin_state_path(@state) do |f| %>
                <%= render 'shared/error_state_messages' %>
                <%= render partial: "form", locals: { state: @state, f: f, mode_text: 'UPDATE' } %>
              <% end %>
            </section>
          </div>
          <!-- /content-panel -->
        </div>
        <!-- /col-lg-4 -->
      </div>
      <!-- /row -->
    </section>
    <!-- /wrapper -->
  </section>
  <!-- /MAIN CONTENT -->
  <!--main content end-->
</section><!--/container-->