<section id="container">
  <%= render 'layouts/admin_sidebar' %>
  <!-- **********************************************************************************************************************************************************
      MAIN CONTENT
      *********************************************************************************************************************************************************** -->
  <!--main content start-->
  <section id="main-content">
    <section class="wrapper">
      <h3><i class="fa fa-angle-right"></i> States</h3>
      <div class="row mt">
        <div class="col-lg-12">
          <div class="content-panel">
            <h4><i class="fa fa-angle-right"></i>State list</h4>
            <div style="margin: 0 10px 10px 0;text-align: right;">
              <%= link_to "New", new_admin_state_path, class:"btn btn-outline-secondary btn-sm"%>
            </div>
            <section id="unseen">
              <table class="table table-bordered table-striped table-condensed">
                <thead>
                  <tr>
                    <th>#</th>
                    <th class="numeric">Name</th>
                    <th class="numeric">Board</th>
                    <th class="numeric" colspan="2">Manage</th>
                  </tr>
                </thead>
                <tbody>
                  <% @states.each do |state| %>
                  <tr>
                    <td><%= link_to state.id, admin_state_path(state) %></td>
                    <td><%= state.name %></td>
                    <td>
                      <%= link_to "#{state.board.id} / #{state.board.name}", admin_board_path(state.board) %>
                    </td>
                    <td class="numeric">
                      <%= link_to "Edit", edit_admin_state_path(state) %>
                    </td>
                    <td class="numeric">
                      <%= link_to "Delete", admin_state_path(state), method: :delete , data: { confirm: "You sure?" }%>
                    </td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
            </section>
          </div>
          <!-- /content-panel -->
        </div>
        <!-- /col-lg-4 -->
      </div>
      <!-- /row -->
    </section>
    <!-- /wrapper -->
  </section>
  <!-- /MAIN CONTENT -->
  <!--main content end-->
</section><!--/container-->