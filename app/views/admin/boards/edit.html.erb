<section id="container">
  <%= render 'layouts/admin_sidebar' %>
  <!-- **********************************************************************************************************************************************************
      MAIN CONTENT
      *********************************************************************************************************************************************************** -->
  <!--main content start-->
  <section id="main-content">
    <section class="wrapper">
      <h3><i class="fa fa-angle-right"></i><%= link_to "Boards", admin_boards_path%></h3>
      <div class="row mt">
        <div class="col-lg-12">
          <div class="content-panel">
            <h4><i class="fa fa-angle-right"></i>Edit Board</h4>
            <div style="margin: 0 10px 10px 0;text-align: right;">
              <%= link_to "Board Infomation", admin_board_path(@board), class:"btn btn-outline-secondary btn-sm"%>
            </div>
            <section id="unseen">
              <%= form_with model: @board, local: true, url: admin_board_path(@board) do |f| %>
                <%= render 'shared/error_board_messages' %>
                <%= render partial: "form", locals: { board: @board, members: @members, f: f, mode_text: 'UPDATE' } %>
              <% end %>
            </section>
          </div>
          <!-- /content-panel -->
        </div>
        <!-- /col-lg-4 -->
      </div>
      <!-- /row -->
    </section>
    <!-- /wrapper -->
  </section>
  <!-- /MAIN CONTENT -->
  <!--main content end-->
</section><!--/container-->